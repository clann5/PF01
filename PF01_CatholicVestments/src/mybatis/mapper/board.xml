<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mybatis.mapper.board">

	<!-- 1. SelectList -->
  <select id="selectBoardList" resultType="dto.BoardDto">
	SELECT	NO
		,	TITLE
		,	USER_ID
		,	CONTENT
		,	BOARDABLE
		,	WRITE_DATE	
		,	FILENAME
		,	PW
		,	HIT
	FROM	BOARD
  </select>
  	<!-- 2. SelectOne -->
  <select id="selectBoardByNo" parameterType="int" resultType="dto.BoardDto">
  	SELECT	NO
		,	TITLE
		,	USER_ID
		,	CONTENT
		,	BOARDABLE
		,	WRITE_DATE
		,	FILENAME
		,	PW
		,	HIT
	  FROM	BOARD
	 WHERE	NO = #{no}
  </select>	
  	
  	<!-- 3. Insert -->
  <insert id="insertBoard" parameterType="dto.BoardDto">
  	INSERT
  	  INTO	BOARD(NO, TITLE, USER_ID, CONTENT, FILENAME, PW)
  	VALUES	(BOARD_SEQ.NEXTVAL, #{title}, #{user_id}, #{content}, #{filename}, #{pw} )
  </insert>	
  	<!-- 4. Update -->
  <update id="updateBoard" parameterType="dto.BoardDto">
  	UPDATE	BOARD
 	<if test="filename == null">
  	   SET	TITLE = #{title}, CONTENT = #{content}
  	</if>
  	<if test="filename != null">
  	   SET	TITLE = #{title}, CONTENT = #{content}, FILENAME = #{filename}
  	</if>
  	 WHERE	NO = #{no}
  </update>
  
  <!-- 5. Delete -->
  	<delete id="deleteBoard" parameterType="int">
  	DELETE
  	  FROM	BOARD
  	 WHERE	NO = #{no}
  	</delete>
  
  <!-- 6. Hit -->
  <update id="updateHit" parameterType="int">
  	UPDATE	BOARD
  	   SET	HIT = HIT + 1
  	 WHERE NO = #{no}
  </update>
</mapper>